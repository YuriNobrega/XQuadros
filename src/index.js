import React from 'react';
import ReactDOM from 'react-dom';
//import './index.css'; // se existir um arquivo de estilo
import App from './App.jsx'; // componente principal do aplicativo

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);

